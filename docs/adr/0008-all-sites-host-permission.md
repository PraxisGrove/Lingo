---
status: accepted
---

# 申请所有网站的主机权限

Lingo 首版申请 `<all_urls>`，因为默认自动站点、源语言自动翻译、页面加载前后的正文识别、动态内容和 iframe 翻译都不能只依赖用户手势授予的 `activeTab`。该权限仅用于识别与呈现翻译内容，不得用于收集浏览历史；网页正文只能发送给用户明确选择的翻译服务，密码、支付和其他受保护区域始终排除。
